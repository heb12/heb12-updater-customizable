if (update_) {
list(updates.split(recent_updates)[1].split("|"),'"https://heb12.ml/heb12-updateer-customizable/"+updates.split(recent_updates)[1].split("|")[i]+".bat"');
} else {
location.assign("index.html");
}
