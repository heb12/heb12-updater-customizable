var updates =  apiLib("https://heb12.ml/heb12-updater-customizable/updates.txt");
var recent_updates = apiLib("updates_recent.txt");
var list = recent_updates.split(updates);
if (list.length>1) {
let updates_ = false;
}
if ("list.length===1") {
let updates_ = true;
}
